一、必做题
1.全局路由和子路由的作用以及路由匹配规则
（1）作用：解决路由数量较多时，主路由太过繁杂
（2）匹配规则：
1）从主路由列表的第一个元素开始（从上到下）开始匹配
2）一旦匹配成功，会自动调用path第二个参数锁指定的视图函数
3）一旦匹配成功之后，不会再往下匹配
4）如果匹配不成功，会返回一个状态码为404的页面
5）url路由信息，推荐使用斜杠（/）结尾

2.搭建Django工程
提示：

	a.创建一个应用, 并通过视图(函数视图或者类视图)来展示内容

	b.把演练效果录屏上传(推荐)或者将效果截图上传